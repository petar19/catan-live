// Placeholder for the admin games list (CLAUDE.md Phase 3). Will read
// games/* from Firestore (admin-only, per firestore.rules) and link to a
// per-game detail/chart view.
export function GamesList() {
  return (
    <div>
      <h1>Games</h1>
      <p>Coming soon — admin game list + per-game charts (Phase 3).</p>
    </div>
  );
}
